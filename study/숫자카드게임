#숫자카드게임
#n*m(세로*가로)
#뽑고자 하는 카드 포함된 행 선택 -> 그 중 가장 낮은 숫자 카드 뽑음
def solution(matrix):
    answer = 0
    for i in matrix:
        if answer < min(i):
            answer = min(i)
    return answer
n,m = map(int,input().split())
matrix = []
for i in range(n):
    matrix.append(list(map(int,input().split())))
print(solution(matrix))
